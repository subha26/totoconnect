
/**
 * Asynchronously sends an OTP to a phone number.
 *
 * @param phoneNumber The phone number to send the OTP to.
 * @param otp The OTP to send.
 * @returns A promise that resolves to true if the OTP was sent successfully.
 */
// export async function sendOtp(phoneNumber: string, otp: string): Promise<boolean> {
//   // This is now handled by Firebase Phone Authentication.
//   // Keeping the file for now in case other SMS services are needed later.
//   console.log(`Mock OTP send to ${phoneNumber}: ${otp}`);
//   return true;
// }
